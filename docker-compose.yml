services:
  playcha:
    image: ghcr.io/mjlescano/playcha:latest
    ports:
      - "8191:8191"
    environment:
      - LOG_LEVEL=info
      - HEADLESS=true
      - TZ=UTC
      # - BROWSER=camoufox
      # - CAPTCHA_SOLVER=click
      # - TWO_CAPTCHA_API_KEY=
      # - PROXY_URL=
      # - PROXY_USERNAME=
      # - PROXY_PASSWORD=
    restart: unless-stopped
